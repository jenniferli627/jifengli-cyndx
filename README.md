# Demo app for MUI React TS

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## To start

- `yarn install`
- `yarn start`

## Note:

- This is for demo only, it does not build as a library.